package constant

object NavigationConstant {
    enum class ActivityClass(val className: String) {
        HOME_ACTIVITY("page.MainActivity"),
        MOVIE_DETAIL_ACTIVITY("page.MovieDetailActivity"),
    }
}